{
  "name": "sizeOf",
  "main": "index.js",
  "scripts": {
    "configure": "node-gyp configure",
    "build": "node-gyp build",
    "test": "node-gyp build && mocha"
  },
  "devDependencies": {
    "mocha": "^2.2.5",
    "nan": "^1.8.4",
    "node-gyp": "^1.0.3"
  },
  "dependencies": {
    "bindings": "^1.2.1"
  }
}
